/**
 * For 'packed_test.exe', but not yet.
 */
#include "mongoose.h"

const char *mg_unlist_1 (size_t i);
const char *mg_unpack_1 (const char *name, size_t *size, time_t *mtime);

const char *mg_unlist_2 (size_t i);
const char *mg_unpack_2 (const char *name, size_t *size, time_t *mtime);

int main (int argc, char **argv)
{
  (void) argc;
  (void) argv;
}
